export default function () {
class Emoji {
  constructor () {
    this.emoji = random(Emojis.tag.people)
  }
}
  
Layers.create(() => {
  new Layer({
    id: 'crowd',

    menu: {
      size: {min: 100, max: 500}
    },
    
    setup () {
      for (let i = 0; i < $size; i++) {

      }
    }
  })
})
}